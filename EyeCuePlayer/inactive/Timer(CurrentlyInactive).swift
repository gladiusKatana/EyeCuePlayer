// eyeCuePlayer ･ Timer(CurrentlyInactive).swift
import UIKit

extension VideoVC {

    //    func earlyPlayButtonAnimate() {
    //        if (abs(delT - corFac - moreFac) ..< abs(delT - corFac)).contains(abs(timeAccumulated)) {
    //            if !playButtonOn {
    //                animatePlayButton()
    //                ///playButtonOn = true
    //            }
    //        }
    //    }
    
    //    func repeatStopwatch() {
    //        playbackRepeatStopwatch(delay: 0.0, closure: { [weak self] () -> () in
    //
    //            if let strongSelf = self {
    //                strongSelf.playbackTimer(localT: 1.0)
    //            }
    //        })
    //    }

}

