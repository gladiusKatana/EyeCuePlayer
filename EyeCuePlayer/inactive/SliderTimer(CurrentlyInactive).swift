// eyeCuePlayer ･ SliderTimer(CurrentlyInactive).swift
import UIKit

extension VideoVC {
    
    //    func sliderMotionTimer(localT: Double) {  //print("------------------------------------------------------ 🕛sliderMOtionTimer")
    //
    //        if (!donePlaying && started) || mode == "reverse" || justInterrupted {
    //            if (!interrupted || justInterrupted) && !justFSeeked {
    //
    //
    //                tCatchForSlider = CFAbsoluteTimeGetCurrent()
    //                delayInterForSlider = tCatchForSlider - tThrowForSlider
    //
    //                if !downPaused {timeAccumulatedForSlider = timeAccumulatedForSlider + delayInterForSlider * localSpeed}
    //
    //                if !paused || mode == "reverse" {
    //
    //                    if mode == "forward" {
    //                        ecks = x + 0                                // +  0
    //                    }
    //                    else if mode == "reverse" {
    //                        ecks = x + 2                                // +  2
    //                    }
    //
    //                    videoPlayerView.frameSlider.setValue(Float((times[ecks] + timeAccumulatedForSlider)/levelLength), animated: true)
    //                }
    //
    //                tThrowForSlider = CFAbsoluteTimeGetCurrent()
    //                //if hourGlassOn {
    //                ///hourglass()
    //                //}
    //
    //                sliderRepeatStopwatch(delay: 0.0, closure: { [weak self] () -> () in
    //                    if let strongSelfForTimer = self {
    //                        strongSelfForTimer.sliderMotionTimer(localT: 1.0)
    //                    }
    //                })
    //
    //            } //else {print("\n** interrupted = \(interrupted) & justFSeeked = \(justFSeeked)")}
    //        } //else {print("\n** donePlaying = \(donePlaying) & paused = \(paused) & mode = \(mode) -------------------------------------")}
    //    }
    
    //    func sliderRepeatStopwatch(delay:Double, closure:()->()) {                                              // D i s p a t c h
    //        DispatchQueue.main.asyncAfter(deadline: .now() + 0.05) {
    //            self.sliderMotionTimer(localT: 1.0)
    //        }
    //    }
    
}

