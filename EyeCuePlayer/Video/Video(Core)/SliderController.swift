// eyeCuePlayer ･ SliderController.swift
import UIKit

class SliderController: NSObject /*, UIGestureRecognizerDelegate*/ {
    
    //        lazy var sliderView: SliderView = {   //! sliderView must be global only or else constraints bug
    //            let sliderView = SliderView()
    //            return sliderView
    //        }()=
    
    //    func sliderControllerShowVideoPlayer() {  ///print("times = \(times)");  print("...timeD = \(timeD)")
    //
    //        justPinched = false
    //        times.append(position)
    //
    //        backgroundView.backgroundColor = .clear
    //
    //        if justPressedSliderButton {
    //            backgroundView.addGestureRecognizer(UIPinchGestureRecognizer(target: self, action: #selector(calibrationPinch)))
    //            backgroundView.addGestureRecognizer(UITapGestureRecognizer(target: self, action: #selector(calibrationTap)))
    //        }
    //
    //        globalWindow.addSubview(backgroundView)                                                   //----------------------------addSubviews
    //        backgroundView.frame = globalWindow.frame
    //
    //        let videoPlayerFrame = backgroundView.frame
    //        sliderView = SliderView(frame: videoPlayerFrame)
    //
    //        backgroundView.addSubview(sliderView)
    //        sliderView.calibrationSlider.value = 0.0
    //
    //        if justPressedSliderButton {playor.seek(to: CMTime(seconds: 0, preferredTimescale: 100), toleranceBefore: CMTime.zero, toleranceAfter: CMTime.zero)}
    //    }
    
}

